#include "main.h"

/**
* print_alphabet - Prints all alphabet in lower case.
*/

void print_alphabet(void)
{
	char alphabet;

	for (alphbeta = 'a'; alphabet < 'z'; aplhabet++)
		_putchar(alphabet);
	_putchar('\n';)
}
